import Layout from "../../components/layout";

const Home: Component = () => {
  return (
    <Layout>
      Go to blogs to experiment
      <div>Missing: Input Validation, UX States</div>
    </Layout>
  );
};

export default Home;
